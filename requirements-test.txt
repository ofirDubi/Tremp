# Test dependencies for Tremp

# Core testing framework
pytest>=7.0.0
pytest-cov>=4.0.0

# Mocking
unittest-mock>=1.0.0  # Included in Python stdlib, but listed for documentation

# Optional: async testing support (if needed in future)
# pytest-asyncio>=0.21.0

# Optional: test performance profiling
# pytest-benchmark>=4.0.0
